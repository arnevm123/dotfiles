# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!


set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# default_border pixel
# default_floating_border pixel
font pango:monospace 0
titlebar_border_thickness 3
smart_borders on
# titlebar_border_thickness 1

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex-autostart --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork


# Use pactl to adust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
# tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec foot

bindsym $mod+b exec flatpak run one.ablaze.floorp
bindsym $mod+n exec slack
bindsym $mod+Shift+p exec wlogout

# workspace number $ws5 \
# exec slack \

bindsym $mod+z exec makoctl dismiss -a

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id dmenu
# A more modern dmenu replacement is rofi:
# bindsym $mod+d exec --no-startup-id rofi -show run  -m -1
# A more modern dmenu replacement is fuzzel:
bindsym $mod+d exec fuzzel

# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+p split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent
bindsym $mod+Shift+a focus child

set $screenshot 1 sel, 2 whole, 3 sel clip, 4 whole clip, 5 swappy
mode "$screenshot" {
    bindsym 1 exec 'grim -g "$(slurp)" ~/ps_$(date +"%Y%m%d%H%M%S").png', mode "default"
    bindsym 2 exec 'grim ~/ps_$(date +"%Y%m%d%H%M%S").png', mode "default"
    bindsym 3 exec 'grim -g "$(slurp)" - | wl-copy', mode "default"
    bindsym 4 exec 'grim - | wl-copy', mode "default"
    bindsym 5 exec 'grim -g "$(slurp)" - | swappy -f -', mode "default"

# back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+s mode "default"
}

bindsym $mod+s mode "$screenshot"
bindsym $mod+Shift+s exec 'grim -g "$(slurp)" - | swappy -f - -o - | wl-copy ', mode "default"

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+Shift+o \
workspace $ws1; \
exec firefox; \
exec flatpak run com.slack.Slack; \
exec flatpak run io.github.mahmoudbahaa.outlook_for_linux ;\
exec flatpak run com.spotify.Client; \
workspace $ws3; \
exec foot

# default desktops
assign [class="Slack"] workspace number $ws5
assign [class="outlook-for-linux"] workspace number $ws5
assign [class="Spotify"] workspace number $ws7
# assign [class="floorp"] workspace number $ws8

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+r reload
bindsym $mod+semicolon exec copyq toggle
for_window [app_id="copyq"] floating enable
# do not focus preview window on open
no_focus [title="^Peek preview$"]
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'swaymsg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
		bindsym h resize shrink width 10 px or 10 ppt
		bindsym j resize grow height 10 px or 10 ppt
		bindsym k resize shrink height 10 px or 10 ppt
		bindsym l resize grow width 10 px or 10 ppt
# same bindings, but for the arrow keys
		bindsym Left resize shrink width 10 px or 10 ppt
		bindsym Down resize grow height 10 px or 10 ppt
		bindsym Up resize shrink height 10 px or 10 ppt
		bindsym Right resize grow width 10 px or 10 ppt

# back to normal: Enter or Escape or $mod+r
		bindsym Return mode "default"
		bindsym Escape mode "default"
		bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
bindsym --locked {
    XF86AudioRaiseVolume exec \
        'pactl set-sink-volume @DEFAULT_SINK@ +${STEP:-5}% && ~/.config/sway/scripts/audio-notifications.sh'
    XF86AudioLowerVolume exec \
        'pactl set-sink-volume @DEFAULT_SINK@ -${STEP:-5}% && ~/.config/sway/scripts/audio-notifications.sh'
    XF86AudioMute exec \
		'pactl set-sink-mute @DEFAULT_SINK@ toggle && ~/.config/sway/scripts/audio-notifications.sh'
    XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
}

bindsym --locked {
	XF86MonBrightnessUp exec \
		'busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateBrightness d +0.02 && ~/.config/sway/scripts/wl-brightness.sh'
	XF86MonBrightnessDown exec \
		'busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateBrightness d -0.02 &&  ~/.config/sway/scripts/wl-notifications.sh'
	$mod+Control+Minus exec \
		'busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n -100 && ~/.config/sway/scripts/wl-temp.sh'
	$mod+Control+Equal exec \
		'busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n +100 && ~/.config/sway/scripts/wl-temp.sh'
	$mod+Control+0 exec \
		'busctl --user set-property rs.wl-gammarelay / rs.wl.gammarelay Brightness d 1.0 && busctl --user set-property rs.wl-gammarelay / rs.wl.gammarelay Temperature q 6500 && ~/.config/sway/scripts/wl-temp.sh'

	$mod+Control+Underscore exec \
		'busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateGamma d -0.02 && ~/.config/sway/scripts/wl-gamma.sh'
	$mod+Control+Plus exec \
		'busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateGamma d +0.02 && ~/.config/sway/scripts/wl-gamma.sh'
}

# # Sreen brightness controls -- # https://github.com/haikarainen/light
# # increase screen brightness
# bindsym XF86MonBrightnessUp exec light -A 5 && ~/.config/sway/scripts/brightness-notifications.sh
# # decrease screen brightness
# bindsym XF86MonBrightnessDown exec light -U 5 && ~/.config/sway/scripts/brightness-notifications.sh

# multimedia keys
bindsym XF86AudioPlay  exec "mpc toggle"
bindsym XF86AudioStop  exec "mpc stop"
bindsym XF86AudioNext  exec "mpc next"
bindsym XF86AudioPrev  exec "mpc prev"
bindsym XF86AudioPause exec "mpc pause"
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

# Startup programs
exec --no-startup-id mpd
exec --no-startup-id copyq
exec --no-startup-id kanshi
# exec_always feh --bg-scale ~/Pictures/bestFriend-wallpaper-07-1920x1200.png
exec_always swaybg -i ~/dotfiles/background/default.png -m fill
exec --no-startup-id mako --layer overlay --icons 0
exec --no-startup-id nm-applet --indicator
exec --no-startup-id blueman-applet
exec --no-startup-id "dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway"

# bindsym $mod+Shift+p      mode $power_mode
# mode $power_mode {
#     bindsym p         exec systemctl poweroff
#     bindsym r         exec systemctl reboot
#     bindsym l         exec systemctl logout
#     bindsym q         exec --no-startup-id swaymsg exit, mode "default"
#     bindsym h         exec systemctl hibernate
#     # bindsym l         exec swaylock, mode "default"
#     # bindsym s        exec sudo  /lib/systemd/systemd-sleep hybrid-sleep
#     bindsym s         exec pm-suspend
#
#     bindsym Return    mode "default"
#     bindsym Escape    mode "default"
# }

input * accel_profile flat

# input "type:keyboard" {
# 	 # xcape -e 'Caps_Lock=Escape'
# 	xkb_options caps:escape
# }

# output HDMI-A-2 resolution 2560x1440@59.951hz
# output eDP-1 scale 0.8

# define display output names
set $disp1 "eDP-1"
set $disp2 "DP-2"
set $disp3 "HDMI-A-2"

# default display outputs for workspaces with fallback to disp1
workspace 1 output $disp2 $disp3
workspace 2 output $disp2 $disp3
workspace 3 output $disp2 $disp3
workspace 4 output $disp2 $disp3
workspace 5 output $disp2 $disp3
workspace 6 output $disp2 $disp3
workspace 7 output $disp2 $disp3
workspace 8 output $disp2 $disp3
workspace 9 output $disp2 $disp3
workspace 10 output $disp1

input type:touchpad {
       dwt enabled
       tap enabled
       natural_scroll enabled
       middle_emulation enabled
}

exec swaymsg workspace '1'

exec ~/.cargo/bin/wl-gammarelay-rs

bar {
        swaybar_command waybar
}

bindswitch --reload --locked lid:on exec ~/bin/lidclose.sh
bindswitch --reload --locked lid:off output eDP-1 enable
